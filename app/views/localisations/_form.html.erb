<%= form_for(@localisation) do |f| %>
  <% if @localisation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@localisation.errors.count, "error") %> prohibited this localisation from being saved:</h2>

      <ul>
      <% @localisation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
